document.getElementById("searchButton").addEventListener("click", function() {
    // Define the URL to search
    var url = "http://localhost:50051";

    // Open the URL in a new tab/window
    window.open(url, "_blank");
});
